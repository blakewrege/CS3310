import java.awt.event.FocusAdapter;
import java.io.FileNotFoundException;
import java.io.IOException;
import java.io.RandomAccessFile;
import java.util.ArrayList;
import java.util.Formatter;

public class HashTable {
	private short n;
	private short maxId = 0;
	int MAX_SIZE = 499;
	int IdArr[] = new int[MAX_SIZE];
	String CodeArr[] = new String[MAX_SIZE];

	// Insert used by the Setup Program
	public void insert1Country(String code, short id, int count) throws IOException {
		CodeArr[count] = code;
		IdArr[count] = id;

	}

	public void Hash1(int count, int maxNHomeLoc, int ColRes, UIoutput output) {
		int hashnum = 0;
		for (int i = 0; i < count; i++) {
			
			hashnum = CodeArr[i].charAt(0)*CodeArr[i].charAt(1)*CodeArr[i].charAt(2);
			
			System.out.println(CodeArr[i] + "  " + IdArr[i] + " "+ hashnum);

		}
	}
	
	
	
	
	public void Hash2(int count, int maxNHomeLoc, int ColRes, UIoutput output) {
		int hashnum = 0;
		for (int i = 0; i < count; i++) {
			
			hashnum = CodeArr[i].charAt(0)+CodeArr[i].charAt(1)+CodeArr[i].charAt(2);
			
			System.out.println(CodeArr[i] + "  " + IdArr[i] + " "+ hashnum);

		}
	}
	
	public void Hash3(int count, int maxNHomeLoc, int ColRes, UIoutput output) {
		String hashstr = "";
		int hashnum = 0;
		for (int i = 0; i < count; i++) {
			ha
			hashnum = CodeArr[i].charAt(0)+CodeArr[i].charAt(1)+CodeArr[i].charAt(2);
			
			System.out.println(CodeArr[i] + "  " + IdArr[i] + " "+ hashnum);

		}
	}
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	

	public void ReadAll(int count) {
		for (int i = 0; i < count; i++) {
			System.out.println(CodeArr[i] + "  " + IdArr[i]);

		}

	}

}
